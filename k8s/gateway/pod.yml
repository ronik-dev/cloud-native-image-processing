apiVersion: v1
kind: Pod
metadata:
  name: gateway
  namespace: imageprocessing
spec:
  containers:
    - name: gateway
      image: imageprocessing/gateway:latest
      imagePullPolicy: Never
      envFrom:
        - configMapRef:
            name: gateway-config
