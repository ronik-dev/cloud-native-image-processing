image: maven:3.9-eclipse-temurin-21

stages: 
- test

test-job:
  stage: test
  cache:
    key: "$CI_PROJECT_ID-maven"
    paths:
      - .m2/repository
  variables:
    MAVEN_OPTS: "-Dmaven.repo.local=.m2/repository"
  script: mvn test -B
